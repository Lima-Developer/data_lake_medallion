name: 'lake'
version: '1.0.0'

profile: 'lake'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
docs-paths: ["docs"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  lake:
    silver:
      +materialized: table
    gold:
      +materialized: table
